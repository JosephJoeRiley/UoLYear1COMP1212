//This is a collection of 4 registers in a line.
//A register is a combination of a MUX gate and a
//DFF flip flop. 

CHIP Shift8Bit {
    IN x;
    OUT out[8];

    

    PARTS:
    Register(in=x, load=1, out=out[t]);
}